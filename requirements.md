# requirements.md

## 1. Goal

ローカルに保存された ComfyUI 生成画像を一覧・閲覧し、生成パラメータとプロンプトを確認・コピーできる管理ソフトを作成する。

---

## 2. Problem

### 2-1. 利用形態

- 利用者は本人のみ。ローカル環境で動作させる。
- 認証・アカウントの概念を持たない。
- バックエンドは JSON を返す API とし、フロントエンドから利用する。

### 2-2. 画像の取り込み

- 利用者が指定したルートフォルダ配下を再帰的にスキャンして取り込む。
- アップロード機能は持たない。画像ファイルの実体はスキャン元フォルダに置かれたままとし、ソフト側へコピー・移動しない。
- 取り込んだ画像のファイル名は、スキャン時に統一した命名規則（ファイル更新日時 + ランダム ID）へ揃える。ファイルの内容と置き場所（フォルダ）は変更しない。
- 対象は PNG とする。
- 同一ファイルが重複登録されないこと。ファイルは移動やリネームをされる可能性があるため、パスではなくファイル内容のハッシュを同一性の基準とする。

### 2-3. メタデータの前提

- 生成情報は画像に埋め込まれた ComfyUI のメタデータから自動抽出する。手入力はしない。
- ComfyUI は PNG の tEXt チャンクに以下を格納する。
  - `prompt`: API 形式のノードグラフ JSON
  - `workflow`: UI 形式のノードグラフ JSON
- プロンプト文字列は単独のフィールドとして格納されていない。テキストは `CLIPTextEncode` 系ノードの入力値として存在し、そのノードが `KSampler` 系ノードの `positive` / `negative` のどちらの入力に接続されているかによって正負が決まる。したがって抽出にはグラフの探索が必要となる。
- 生成パラメータの所在も同様にノード依存である。
  - モデル名: `CheckpointLoaderSimple` 等のローダーノード
  - seed / steps / cfg / sampler_name / scheduler / denoise: `KSampler` 系ノード
  - 幅 / 高さ: `EmptyLatentImage` 等のノード
- ワークフローの構成は利用者ごと・生成ごとに自由であり、上記ノードが存在しない構成、複数存在する構成、カスタムノードに置き換えられている構成がありうる。**抽出失敗は異常ではなく通常起こりうる事象として扱う。**
- ComfyUI 以外で生成された画像や、メタデータを持たない画像も同じフォルダに混在しうる。

### 2-4. 表示・操作

- 画像ごとに「ファイル情報」「生成パラメータ」「プロンプト」を閲覧できる。
  - ファイル情報: ファイル名、パス、ファイルサイズ、解像度、ファイル更新日時
  - 生成パラメータ: モデル名、seed、steps、cfg、sampler、scheduler
  - プロンプト: positive / negative
- プロンプトは再利用目的でコピーされる。**表示用の整形や改行の除去を行わず、抽出した原文のまま保持・返却する。**
- 画像ごとにお気に入りのオン/オフを設定でき、お気に入りのみに絞り込める。
- お気に入りの設定はソフト側が保持する情報であり、画像ファイル自体を書き換えない。

### 2-5. 一覧画面の前提

- 一覧は数千件規模を扱う。全件を一度に取得・描画しない。
- 一覧は利用者のスクロールに応じて続きを読み込む。ページ番号による移動は行わない。
- 取り込んだ画像はフォルダで分類されていることが多い。フォルダ階層を辿って絞り込めることを前提とする。
- 一覧のサムネイル表示サイズを利用者が変更できる。
- 並び順はファイル更新日時の降順で固定とする。

### 2-6. サムネイル

- 一覧表示は多数の画像を同時に並べるため、原寸画像をそのまま配信すると表示が成立しない。一覧にはサムネイルを用いる。
- サムネイルは WebP 形式で生成する。
- サムネイルは生成物であり、元画像とは混在させない。スキャン元ルートフォルダ直下の専用ディレクトリにまとめ、その配下はスキャン対象から除外する。
- 生成は毎回行わず、一度生成したものを再利用する。
- 破損ファイルや非対応データが混在しうるため、**個々のサムネイル生成の失敗によってスキャン全体が停止しないこと。**

### 2-7. タグ辞書

- 生成パラメータとは別に、プロンプトに書かれた語そのものを手がかりに画像を探せるようにする。
- 語の意味づけには danbooru のタグ一覧を用いる。利用者が CSV をアップロードして取り込む。列は `tag` / `category` / `category_name` / `post_count` / `created_at` / `aliases` / `other_names` / `posts_url` / `wiki_url` / `has_wiki` である。
- 辞書は 100 万行を超える規模になる。一覧に全件を並べることはできず、検索して引く前提とする。
- プロンプトの語と辞書の照合は完全一致のみとする。表記ゆれや部分一致による推測は行わない。照合できない語が残るのは通常起こりうる事象として扱う。
- CIVITAI から入手した LoRA のトリガーワードのように danbooru に存在しない語がある。これらは辞書へ追加することで同じように扱えるようにする。
- 辞書はプロンプトの語を解説する用途にも使う。区分・投稿数・別名・和名を参照できることが目的に含まれる。

---

## 3. Scope

### In Scope

- ルートフォルダの再帰スキャンと画像の登録
- ComfyUI メタデータの抽出と保存
- WebP サムネイルの生成と配信
- フォルダ階層の取得とフォルダ単位の絞り込み
- 画像一覧の取得（スクロール追従の逐次取得、お気に入り絞り込み）
- 一覧のサムネイル表示サイズ変更
- 画像詳細の取得
- 画像本体の配信
- お気に入りの設定・解除
- LoRA 管理（LoRA ファイルの一覧、画像で使用された LoRA の記録、Trigger Words・メモの保存）
- タグ辞書の取り込み（CSV アップロード）と検索
- プロンプトと辞書の照合、タグによる絞り込み、プロンプト中の語の辞書表示

### Out of Scope

- 画像の生成、編集、削除（リネームは FR-40 のスキャン時自動リネームのみ行い、任意の名前への変更は対象外）
- 画像のアップロード
- 複数ユーザー対応、認証、権限管理
- A1111 / NovelAI など ComfyUI 以外のメタデータ形式への対応
- 画像ごとの手動タグ付け、コレクション、フォルダ分類の編集（プロンプトからの自動的なタグ抽出は FR-48 で行う）
- 表記ゆれの吸収、部分一致や類似語によるタグの推測
- 評価、カラー分類、スマートフォルダ、ゴミ箱
- プロンプトの全文検索（タグによる絞り込みは FR-50 で行う）
- 並び順の切り替え
- 動画・PNG 以外の画像形式
- 外部公開・インターネット経由でのアクセス

---

## 4. Functional Requirements

### スキャン・登録

- FR-1: 指定されたルートフォルダ配下を再帰的に走査し、PNG ファイルを検出する。
- FR-2: スキャンは利用者の明示的な操作で実行できる。
- FR-3: 検出したファイルの内容ハッシュを算出し、未登録のものだけを新規登録する。同一ハッシュのファイルを重複して登録しない。
- FR-4: 登録済み画像と同一ハッシュのファイルが別パスで見つかった場合、既存レコードのパスを更新する。このときお気に入りを含む既存の情報は失われない。
- FR-5: 登録済み画像のファイルがディスク上に存在しなくなった場合、その画像を「不明（missing）」として記録する。レコードおよびお気に入りは削除しない。
- FR-6: スキャン完了時に、走査件数・新規登録件数・更新件数・missing 件数・抽出失敗件数・リネーム件数を返す。
- FR-40: スキャン時、ファイル名が命名規則 `YYYYMMDDTHHMMSS_<uuid4 先頭8桁>.png`（例: `20260227T143052_a3f1c9d2.png`。日時はファイル更新日時のローカルタイム、uuid 部は小文字16進）に合わない PNG を、同一フォルダ内でこの規則の名前にリネームする。ファイル内容・更新日時・フォルダは変更しない。既に規則に合う名前のファイルは変更しない。リネームに失敗しても処理を中断せず、元の名前のまま登録する。この動作は設定 `rename_on_scan` で無効化できる（既定: 有効）。

### メタデータ抽出

- FR-7: PNG の tEXt チャンクから ComfyUI の `prompt` / `workflow` を読み取り、生データとして保持する。
- FR-8: `prompt` のノードグラフを探索し、positive プロンプト、negative プロンプト、モデル名、seed、steps、cfg、sampler、scheduler、生成解像度を抽出する。
- FR-9: プロンプト文字列は抽出時の原文をそのまま保持する。空白・改行の正規化やエスケープを行わない。
- FR-10: 抽出対象のノードが存在しない、複数存在して一意に定まらない、未知のノード構成であるなどの理由で抽出できない項目がある場合、処理を中断せず、当該項目を未取得として登録する。生データは保持する。
- FR-11: 画像ごとに抽出状態を記録する。状態は「全項目抽出成功」「一部のみ抽出」「ComfyUI メタデータなし」の3種を区別できる。
- FR-12: ComfyUI メタデータを持たない PNG も、ファイル情報のみで登録する。エラーとして扱わない。
- FR-13: ファイル情報（ファイル名、パス、ファイルサイズ、画像の幅・高さ、ファイル更新日時）は、メタデータの有無に関わらず取得する。

### 閲覧

- FR-14: 登録済み画像の一覧を取得できる。一覧はページング可能で、ファイル更新日時の降順を既定とする。
- FR-15: 一覧はお気に入りのみに絞り込める。
- FR-16: missing 状態の画像を一覧に含めるかどうかを指定できる。
- FR-17: 画像1件の詳細として、ファイル情報、生成パラメータ、positive / negative プロンプト、抽出状態、お気に入り状態を取得できる。
- FR-18: 画像本体を取得できる。

### お気に入り

- FR-19: 画像のお気に入りをオンにできる。
- FR-20: 画像のお気に入りをオフにできる。
- FR-21: お気に入りの操作によって画像ファイル自体は変更されない。

### サムネイル

- FR-25: 画像の新規登録時にサムネイルを WebP 形式で生成する。
- FR-26: サムネイルは元画像のアスペクト比を維持し、長辺が既定サイズ（768px）以下になるよう縮小する。元画像の長辺が既定サイズ以下の場合は拡大しない。
- FR-27: サムネイルはスキャン元ルートフォルダ直下の専用ディレクトリに出力する。元画像ファイルおよび元画像が置かれたディレクトリ構成を変更しない。
- FR-32: サムネイル専用ディレクトリ配下はスキャン対象から除外する。
- FR-28: サムネイル生成に失敗した画像があっても、スキャン処理を中断しない。当該画像を生成失敗として記録し、残りの画像の処理を継続する。
- FR-29: 既にサムネイルが存在する画像に対して、再スキャン時に再生成を行わない。ただし生成失敗として記録されている画像は再試行する。
- FR-30: 画像ごとのサムネイルを取得できる。
- FR-31: 元画像が missing である画像も、サムネイルが存在する限りサムネイルを取得できる。

### フォルダ・一覧表示

- FR-33: 登録画像が属するフォルダ階層を、ツリー構造として取得できる。階層はスキャン元ルートからの相対位置で表す。
- FR-34: 各フォルダについて、そのフォルダ直下の画像件数と、配下のサブフォルダを含めた合計件数を取得できる。
- FR-35: サムネイル専用ディレクトリ、および `.` で始まるディレクトリは、フォルダ階層に含めない。
- FR-36: 一覧をフォルダで絞り込める。サブフォルダを含めるかどうかを指定できる。
- FR-37: 一覧は続きを逐次取得できる。取得済みの範囲と重複せず、かつ項目を飛ばさない。
- FR-38: 一覧取得時に、現在の絞り込み条件に一致する総件数を取得できる。
- FR-39: 一覧のサムネイル表示サイズを変更できる。最大サイズで表示した場合も、サムネイル画像を元の解像度を超えて引き伸ばさない。

### LoRA 管理

- FR-41: 設定 `lora_root`（任意）配下の LoRA ファイル（`.safetensors` / `.pt` / `.ckpt`。`.` で始まるディレクトリは除外）を利用者の明示的な操作でスキャンし、`lora_root` からの相対パスを名前として登録する。ファイルサイズ・更新日時を記録し、存在しなくなったものは missing とする。レコードは削除しない。
- FR-42: 画像の `prompt` グラフから `lora_name` 入力を持つノードを検出し、画像とその LoRA（および strength_model / strength_clip）を関連付ける。ファイルとして未登録の LoRA は「unknown」として名前のみで登録する。関連付けは画像の登録時と LoRA スキャン時（登録済み画像の保持済み生 JSON から）に行う。
- FR-43: LoRA ごとに Trigger Words とメモを保存・編集できる。これらは LoRA スキャンで上書きされない。
- FR-44: LoRA ごとの使用画像件数を取得でき、一覧を LoRA で絞り込める。画像詳細では使用 LoRA と Trigger Words を参照・コピーできる。

### タグ辞書

- FR-45: danbooru のタグ一覧 CSV を利用者がアップロードして辞書に取り込める。ヘッダ行を持つ CSV とし、`tag` / `category` / `category_name` / `post_count` / `created_at` / `aliases` / `other_names` / `posts_url` / `wiki_url` / `has_wiki` の全列を保持する。取り込みは CSV 由来のレコードを全件置き換える。解釈できない行があっても中断せず、その行を飛ばして件数を報告する。
- FR-46: プロンプト文字列と辞書の照合は、次の正規化を行ったうえでの完全一致とする。
  - カンマで区切って語に分ける
  - 重み指定 `(語:1.2)` および強調のための括弧を取り除き、中の語を取り出す
  - エスケープされた括弧 `\(` `\)` を通常の括弧に戻す
  - アンダースコアを半角空白に置き換える
  - 英字を小文字にする
  - 前後の空白を取り除く
- FR-47: 辞書の `aliases` に完全一致した語は、その別名が属する正式タグとして扱う。ある語が正式名と別名の双方に一致する場合は正式名を優先する。`other_names` は照合の対象としない。
- FR-48: 画像の positive プロンプトから取り出した語を辞書と照合し、一致したタグを画像に関連付ける。negative プロンプトの語は関連付けない。関連付けは画像の登録時および辞書が変化したときに行う。辞書に無い語も画像側に保持し、後から辞書に追加された時点で関連付けられるようにする。
- FR-49: LoRA の Trigger Words をカンマで区切った各語を、LoRA の保存時に辞書へ登録する。この登録は CSV の取り込みで削除されない。辞書上で CSV 由来と区別でき、登録元の LoRA をたどれる。
- FR-50: 一覧をタグで絞り込める。複数のタグを指定でき、すべてを含む条件といずれかを含む条件を切り替えられる。フォルダ・お気に入り・LoRA など他の条件と併用できる。
- FR-51: 辞書を検索できる。取り込んだ全列を対象とし、部分一致で引ける。日本語でも引ける。結果は投稿数の多い順に返す。
- FR-52: 画像詳細で、プロンプト中の各語について辞書の情報（区分・投稿数・別名・和名・リンク）を参照できる。辞書に無い語はその旨が分かる。LoRA のトリガーワード由来の語からは、その LoRA をたどれる。

### 共通

- FR-22: 画像本体の配信を除く全レスポンスは JSON とする。正常系・異常系ともに同様とする。
- FR-23: 存在しない画像 ID を指定した場合は 404 を返す。
- FR-24: 存在しないフォルダをスキャン対象に指定した場合は、登録を一切行わずエラーを返す。

---

## 5. Acceptance Criteria

1. [FR-1, FR-3] 画像を含むフォルダをスキャンすると、配下のサブフォルダ内の PNG も含めて登録される。
2. [FR-3] 同じフォルダを2回スキャンしても、登録件数は増えない。
3. [FR-4] 登録済み画像を別フォルダへ移動して再スキャンすると、登録件数は増えず、該当レコードのパスが新しい位置に更新され、お気に入り状態が維持されている。
4. [FR-5] 登録済み画像をディスクから削除して再スキャンすると、レコードは残ったまま missing となり、お気に入り状態も維持されている。
5. [FR-6] スキャン結果として走査件数・新規登録件数・更新件数・missing 件数・抽出失敗件数が返る。
6. [FR-8, FR-9] 標準的な ComfyUI ワークフローで生成した PNG を登録すると、positive / negative プロンプトが生成時に入力した文字列と完全一致で取得でき、モデル名・seed・steps・cfg・sampler・scheduler が取得できる。
7. [FR-10, FR-11] `KSampler` を含まない、あるいはカスタムノード構成の PNG を登録しても処理が異常終了せず、その画像は「一部のみ抽出」として登録され、取得できた項目のみが返る。
8. [FR-7, FR-10] 抽出に失敗した画像でも、詳細取得時に ComfyUI の生メタデータが参照できる。
9. [FR-12, FR-13] ComfyUI メタデータを持たない PNG を登録すると「ComfyUI メタデータなし」として登録され、ファイル名・サイズ・解像度・更新日時は取得できる。
10. [FR-14] 一覧が既定でファイル更新日時の降順で返る。
11. [FR-19, FR-15] お気に入りをオンにした画像が、お気に入り絞り込みの一覧に含まれる。
12. [FR-20, FR-15] お気に入りをオフにすると、絞り込み一覧から外れる。
13. [FR-21] お気に入りの操作前後で、画像ファイルの内容ハッシュと更新日時が変化しない。
14. [FR-16] missing の画像が、既定の一覧には含まれず、指定時のみ含まれる。
15. [FR-22, FR-23] 存在しない画像 ID を指定した詳細取得が 404 を返し、その `Content-Type` が `application/json` である。
16. [FR-24] 存在しないフォルダを指定したスキャンがエラーを返し、登録済みデータが変化しない。
17. [FR-25, FR-26] スキャン後、新規登録された各画像に WebP 形式のサムネイルが存在し、長辺が 512px 以下かつ元画像とアスペクト比が一致する。
18. [FR-26] 長辺 512px 未満の画像のサムネイルが、元画像と同じ寸法であり拡大されていない。
19. [FR-27, FR-32] スキャン完了後、スキャン元フォルダ配下の PNG ファイルの内容ハッシュの集合と各ファイルの所属フォルダが変化しておらず（FR-40 によるファイル名の変更を除く）、増えたファイルはサムネイル専用ディレクトリ配下のみである。また同じフォルダを再スキャンしてもサムネイルが画像として登録されない。
20. [FR-28] 破損した PNG を含むフォルダをスキャンしても処理が完了し、当該画像は生成失敗として記録され、他の画像のサムネイルは生成されている。
21. [FR-29] 同じフォルダを続けて2回スキャンしたとき、2回目のサムネイル生成件数が 0 件である。
22. [FR-30, FR-31] 元画像を削除して再スキャンした後も、missing を含む一覧で当該画像のサムネイルが取得できる。
23. [FR-33, FR-34] サブフォルダを含むフォルダをスキャンした後、フォルダ階層が入れ子構造で返り、各フォルダに直下件数と配下合計件数が含まれる。
24. [FR-35] フォルダ階層にサムネイル専用ディレクトリが含まれない。
25. [FR-36] サブフォルダを含めない指定でフォルダを絞り込むと、そのフォルダ直下の画像のみが返る。含める指定にすると配下全体の画像が返る。
26. [FR-37, FR-38] 一覧を末尾まで逐次取得したとき、取得した画像 ID に重複がなく、件数が総件数と一致する。
27. [FR-39] サムネイル表示サイズを最大にしたとき、表示サイズがサムネイル画像の実解像度を超えない。
28. [FR-40] 命名規則に合わない名前の PNG をスキャンすると、同じフォルダ内で規則に従った名前になり、内容ハッシュと更新日時は変化しない。登録済み画像がリネームされた場合もレコードは増えず、パスが更新され、お気に入り状態が維持される。同じフォルダを再スキャンしてもファイル名は変わらない。
29. [FR-41, FR-42] `lora_root` に LoRA ファイルを置き、LoRA を使うワークフローの PNG をスキャンしてから LoRA スキャンを実行すると、ファイル由来と workflow 由来の LoRA が名前で統合され、各 LoRA の使用画像件数と画像ごとの使用 LoRA（strength 含む）が取得できる。ファイルを削除して再スキャンすると missing になる。
30. [FR-43, FR-44] Trigger Words とメモを保存すると詳細と画像詳細に反映され、LoRA スキャンを再実行しても保持される。LoRA で絞り込んだ一覧にはその LoRA を使う画像のみが含まれる。

31. [FR-45] タグ CSV をアップロードすると、読み取った行数・取り込み件数・飛ばした行数が返る。同じ CSV を再度アップロードしても件数が二重にならない。
32. [FR-46, FR-52] `masterpiece, (long hair:1.2), \(cosplay\)` のようなプロンプトを持つ画像を登録すると、重み指定やエスケープを含む語も含めて辞書のタグに一致する。画像詳細では各語に辞書の区分・投稿数・和名が添えられ、辞書に無い語はその旨が分かる。
33. [FR-47] 辞書の別名に一致する語を含む画像が、その正式タグで絞り込んだ一覧に含まれる。正式名と別名が競合する語は正式名のタグとして扱われる。和名に一致するだけの語では関連付かない。
34. [FR-48] negative プロンプトにのみ含まれる語で絞り込んでも、その画像は一覧に含まれない。
35. [FR-50] 2 つのタグを「すべて含む」で指定すると両方を含む画像のみが返り、「いずれかを含む」では片方だけの画像も返る。フォルダ絞り込みと併用できる。
36. [FR-49] LoRA の Trigger Words を保存すると辞書に登録され、その語を含む画像をタグで絞り込める。CSV を取り込み直しても登録が残る。
37. [FR-51] 辞書を英語と日本語の部分文字列で検索でき、結果が投稿数の多い順に返る。
38. [FR-48, FR-49] 辞書に無かった語を持つ画像を登録した後にその語を辞書へ追加すると、画像を再スキャンすることなくタグ絞り込みに現れる。

---

## 6. Non-Functional Requirements

- NFR-1: ローカル単一利用者を前提とし、同時アクセスや負荷に関する要件は設けない。
- NFR-2: 画像ファイルの内容はスキャン元から移動・複製・書き換えを行わない。ソフトの動作によって元データが失われないこと。ファイル名の変更は FR-40 に限る。
- NFR-3: 画像 5,000 枚規模のフォルダを対象とした初回スキャンが、現実的な時間内に完了すること。2回目以降のスキャンは、サムネイル再生成が発生しないため初回より大幅に短いこと。
- NFR-4: 一覧表示において、原寸画像は配信しない。
- NFR-5: 100 万行規模のタグ CSV の取り込みが現実的な時間で完了すること。取り込み後において、プロンプトと辞書の照合、タグによる絞り込み、辞書の検索が待たされずに行えること。辞書の追加によって既存画像への関連付けを作り直す処理も同様とする。

---

## 7. 保留事項（次フェーズ以降で検討）

- プロンプトの全文検索（SQLite FTS5 の導入とスキャン時のインデックス同期が必要）
- 並び順の切り替え（カーソル方式の逐次取得と組み合わせるため、並び順ごとにカーソル定義が必要）
- モデル名やサンプラーによる絞り込み（LoRA による絞り込みは FR-44、タグによる絞り込みは FR-50 で実装済み）
- 辞書に載らない語の表記ゆれ吸収、部分一致による候補提示
- ComfyUI 以外のメタデータ形式への対応
- ワークフロー JSON の再利用（ComfyUI への読み込み用エクスポート）
